import { CandidacyUniversity } from './candidacy-university';

describe('CandidacyUniversity', () => {
  it('should create an instance', () => {
    expect(new CandidacyUniversity()).toBeTruthy();
  });
});
